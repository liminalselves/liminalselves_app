<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- WebView 文件选择与 `<input capture>` 拍照 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Android 9 及以下下载文件需要写外部存储；API 29+ 使用 MediaStore/应用私有目录无需此权限 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <!-- Android 13+ 通知权限（阿里云推送 SDK 3.8.4+ 会处理，此处显式声明） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- WebSocket 后台保活：前台服务 + CPU/网络锁，维持 WebView 中的长连接 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- 开启推送时主动申请电池优化豁免，保障常驻原生 WS 不被 Doze/省电策略断网 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:label="阈界人格"
        android:name=".LiminalApplication"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:usesCleartextTraffic="true">
        <!-- WebView 为 PlatformView：Impeller 与 Android PlatformView/纹理组合时常比 Skia 更顿挫（如 flutter/flutter#180831）。全屏 WebView 壳优先 Skia。 -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />
        <!-- Aliyun Push: appkey/appsecret 由 Gradle 从 key.properties（已 gitignore）注入，
             勿在此硬编码真实凭证（本文件被版本控制）。 -->
        <meta-data
            android:name="com.alibaba.app.appkey"
            android:value="${ALIYUN_APP_KEY}" />
        <meta-data
            android:name="com.alibaba.app.appsecret"
            android:value="${ALIYUN_APP_SECRET}" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- WebView 主壳：旋转/键盘等配置变化自行消化不重建（视频全屏转屏不中断、页面不重载）；
             刻意不含 uiMode——系统深浅色切换仍需重建 Activity 以刷新 WebView 主题与壳层联动。 -->
        <activity
            android:name=".NativeWebViewActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:theme="@style/NormalTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|screenLayout|locale|layoutDirection|fontScale|density"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize" />
        <!-- 推送权限设置向导：逐项引导开启通知/电池豁免/悬浮通知。
             使用专用主题（各 values 变体均有定义），保证系统栏与主页壳层一致的浅色。 -->
        <activity
            android:name=".PermissionSetupActivity"
            android:exported="false"
            android:theme="@style/Theme.Liminal.Wizard" />
        <service
            android:name=".KeepAliveService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="http"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
