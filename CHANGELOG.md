# 阈界人格（Liminal Selves）— 更新记录

本文档记录面向测试用户与发布渠道的版本说明。当前包名：`top.liminalselves.app`。

---

## 0.1.9+11 — 深浅色跟随修复与壳层主题联动（2026-09-06）

**对外版本名：** `0.1.9`

**构建号（Android versionCode）：** `11`

### 变更摘要

- **站内深浅色跟随修复：** 修复 App 内 Misskey「跟随设备深浅色」永远读到浅色的问题（浏览器则正常）。根因是 Android WebView 的 `prefers-color-scheme` 并不读系统设置，而是读 WebView 宿主主题的 `isLightTheme`；此前 WebView 挂在强制浅色的 Activity 主题上，站内永远被判定为白天。现在 WebView 以 Material DayNight 主题的 Context 创建，站内可正确跟随系统深浅色（Android 10+ 生效；Android 9 及以下因系统属性缺失仍恒报浅色，站内手动切换主题不受影响）。
- **壳层跟随站内主题：** 外壳 UI 不再固定浅色，改为跟随 Misskey 当前实际生效的主题——站内「跟随设备」或手动选择浅色/深色时，壳层均实时联动，无需重启 App：「即将离开站点」等确认对话框、加载失败错误页、下拉刷新指示器、顶部加载条与初始壳色随站内深浅切换；系统栏颜色本就跟随页面背景色，与站内主题保持一致。
- **权限向导深色适配：** 推送权限设置向导新增暗色配色，进入向导时按当前站内深浅自动选择亮/暗样式，系统栏图标外观同步适配。
- **深色启动防闪白：** 深色模式下 WebView 初始底色同步压黑，消除站点应用深色主题前的白屏闪变。
- **视频全屏修复：** 修复私信视频等媒体的全屏按钮在 App 内无反应的问题（浏览器正常）。站内视频走标准 Fullscreen API，Android WebView 需由宿主实现 `onShowCustomView`/`onHideCustomView` 才能全屏；现已补齐：全屏铺满整屏并沉浸式隐藏系统栏、播放期间保持屏幕常亮，返回键优先退出全屏；全屏中旋转屏幕不再重建页面（旋转等配置变化改由壳层自行消化，深浅色切换时的重建刷新机制不受影响）。

## 0.1.8+10 — 常驻推送、会话导出修复与稳定性（2026-08-28）

**对外版本名：** `0.1.8`

**构建号（Android versionCode）：** `10`

### 变更摘要

- **常驻原生推送：** 新增原生 OkHttp WebSocket 直连 Misskey streaming（main channel，心跳 + 指数退避重连），绕过 WebView JS 在后台被系统节流的问题；推送开关开启且已登录时连接常驻，不随页面生命周期启停，系统通知统一由原生通道触发，避免双通道重复弹窗。
- **通知分级与修复：** 私信走 HIGH 优先级渠道（弹窗 + 声音）、互动通知静默、保活通知最低优先级；支持智能体（AI 角色）消息的系统通知推送；修复群聊推送通知标题显示发送者昵称而非群聊名的问题。
- **权限向导：** 新增 5 步权限引导页（通知总开关、电池豁免、私信/互动/保活渠道），跳转系统设置返回后自动逐项验证，适配国产 ROM「总开关 + 渠道独立」的两层权限结构；向导采用现代 onboarding 风格并修复 Android 12+ 系统栏突变深色。
- **会话导出修复：** 修复智能体会话「导出上下文」在 App 内提示「下载失败: Failed to fetch」的问题。站点前端在触发下载后立即注销 blob URL，导致 App 异步 fetch 时必然失败；现在通过注入 shim 在创建时保留 Blob 引用，下载时直接读取，所有 blob 类下载（如网盘文件）均受益。
- **下载落盘统一：** blob / data URL 下载不再写入应用私有目录，统一与 HTTP 下载一致保存到公共 `Download/` 目录（API 29+ 经 MediaStore 写入、无需权限；Android 9 及以下自动申请存储权限后直写），同名文件自动追加序号；卸载 App 不再影响已下载的文件。
- **导航历史修复：** 修复 WebView 渲染进程被系统回收重建后，返回键导航历史丢失的问题。

## 0.1.6+8 — 手势优化与文件下载（2026-08-02）

**对外版本名：** `0.1.6`

**构建号（Android versionCode）：** `8`

### 变更摘要

- **手势冲突修复：** 下拉刷新引入方向锁定机制，根据滑动轨迹角度判断水平/垂直意图，水平滑动导航栏时不再误触发刷新；加大热区垂直触发阈值（1.8× touch slop），容忍手指自然抖动。
- **返回键体验优化：** SPA 路由前进到板块根路径（首页、探索、通知、消息）时自动清空 WebView 历史栈，返回键表现为"回上级/退出"而非逐页倒带。
- **文件下载支持：** WebView 新增 DownloadListener，支持 HTTP/HTTPS（系统 DownloadManager，通知栏进度）、blob URL（JS fetch + base64 回传）和 data URL 下载；Android 9 及以下自动申请存储权限，API 29+ 免权限写入应用私有目录；文件名冲突自动重命名，下载完成可点击打开。

## 0.1.5+7 — 稳定性与性能优化（2026-07-04）

**对外版本名：** `0.1.5`

**构建号（Android versionCode）：** `7`

### 变更摘要

- **加载失败页：** 新增加载失败界面和重试、浏览器打开操作；仅在 WebView 主框架明确返回网络或 HTTP 错误时显示，不再把启动、刷新和正常加载过程误判为失败。
- **更新跳转：** 修复更新弹窗中“前往更新”无法打开系统浏览器的问题；Android 更新检查迁移到轻量原生启动门控，并加入 3 小时缓存和短超时，减少重复启动等待。
- **启动与 Logo：** 调整普通启动屏及 Android 12 启动图安全边距，避免部分机型裁切 Logo；恢复统一的蓝色 Material 加载动画。
- **WebView 稳定性：** 处理渲染进程异常恢复、页面刷新、文件选择和外部链接跳转；移除重复 HTTP 预探测和空白页轮询，避免额外请求及错误页误报。
- **运行性能：** Android 启动不再预先创建 FlutterEngine；主题颜色同步改为去重、防抖和定向监听，后台网络任务改用共享线程池，高刷新率提示迁移到实际显示的原生 WebView Activity。
- **正式包优化：** 启用 R8 代码压缩和资源裁剪，移除不再使用的 `flutter_displaymode`；Release APK 由约 28.9 MB、双 DEX 降至约 24.0 MB、单 DEX。
- **兼容性：** 补齐 Android 24+ API 保护、可选摄像头声明、Material 主题属性版本标记及阿里云推送 R8 规则。

## 0.1.4+6 — 启动屏、Logo 与 Android 壳（2026-04-25）

**对外版本名：** `0.1.4`  
**构建号（Android versionCode）：** `6`

### 变更摘要

- **启动屏底图：** 将白底由位图拉伸改为纯色，避免在部分 ROM 上刘海/导航区漏边、透出壁纸；Android 12+ 启动主题补全 `windowBackground` 与系统栏色，与系统 SplashScreen 过渡衔接更稳。
- **开屏 Logo：** 按屏幕最小宽度分档（`values` 与 `values-sw*dp`）控制 Logo 容器尺寸，居中显示且等比缩放，减少过小或过大、内容被裁切的情况。
- **外链跳转确认（NativeWebViewActivity）：** 对非 `liminalselves.top` / `*.liminalselves.top` 的 http(s) 链接在系统浏览器打开前需确认。对话框使用 `MaterialAlertDialog`，并通过 `ContextThemeWrapper` 注入完整 Material 3 主题，与站内 Flutter 更新类弹窗层级一致，避免系统怀旧样式，并消除因 Activity 基座非 Material 导致属性解析失败而闪退的问题。
- **返回防误退：** 当 WebView 无历史可退时，首次按返回仅提示（Toast / SnackBar），约 2 秒内再次按返回才结束应用；有历史时仍先页面回退。嵌入 Flutter 的 WebView 壳与全屏原生 WebView 行为对齐。

## 0.1.3+5 — 体验与更新内容增强（2026-04-14）

**对外版本名：** `0.1.3`  
**构建号（Android versionCode）：** `5`

### 变更摘要

- **系统栏一致性：** 上下系统栏常驻显示，并在切后台/回前台、主题变化时与页面背景色持续同步；增加缓存兜底与去重/合并，减少抖动与重复刷新。
- **加载期视觉：** Flutter 开屏与过渡加载阶段统一白色系统栏与白底加载；加载进度条改为 Flutter 风格蓝色。
- **更新内容：** 升级提示弹窗支持展示“高于当前版本”的所有更新内容（由服务器侧 changelog 条目聚合）；站内「设置 → 应用」页可直接查看更新日志列表。
